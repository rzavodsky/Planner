package rzavodsky.planner

/**
 * Stores the ids of preferences
 */
object Preferences {
    /**
     * URI of the org file
     */
    const val orgFile = "org_file"
    /**
     * Default duration of a created plan
     */
    const val defaultDuration = "default_duration"
    /**
     * Default starting hour of a created plan
     */
    const val startHour = "start_hour"

    /**
     * Amount of minutes to notify in advance
     */
    const val notifyAdvance = "notify_advance"
}